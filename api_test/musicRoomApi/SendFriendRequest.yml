info:
  name: SendFriendRequest
  type: http
  seq: 19

http:
  method: GET
  url: https://localhost/friendship/send-friend-request
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
