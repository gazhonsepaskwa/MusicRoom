info:
  name: add_album
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:4242/add_album
  body:
    type: json
    data: |-
      {
        "q" : "Kyougen - Ado"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
