info:
  name: add_artist chill
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:4242/add_artist
  body:
    type: json
    data: |-
      {
        "q" : "FuzeIII"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
