info:
  name: add_artist hard
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:4242/add_artist
  body:
    type: json
    data: |-
      {
        "q" : "Queen"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
