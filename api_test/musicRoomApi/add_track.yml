info:
  name: add_track
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:4242/add_track
  body:
    type: json
    data: |-
      {
        "q" : "Scared of the dark - em beihold"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
