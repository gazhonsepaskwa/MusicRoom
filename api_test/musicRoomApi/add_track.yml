info:
  name: add_track
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:4242/add_track
  body:
    type: json
    data: |-
      {
        "q" : "niki minaj anaconda"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
