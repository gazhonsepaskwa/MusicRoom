info:
  name: deleteAccount
  type: http
  seq: 8

http:
  method: POST
  url: https://localhost:8443/auth/delete-account
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
