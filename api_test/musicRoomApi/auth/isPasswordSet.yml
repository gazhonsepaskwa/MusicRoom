info:
  name: isPasswordSet
  type: http
  seq: 11

http:
  method: GET
  url: https://localhost:8443/users/is-google-checked
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
