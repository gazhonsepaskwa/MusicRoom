info:
  name: login
  type: http
  seq: 6

http:
  method: POST
  url: https://localhost/auth/login
  body:
    type: json
    data: |-
      {
      "username": "monbeaucul",
      "password": "esttresJolie1@"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
