info:
  name: login
  type: http
  seq: 6

http:
  method: POST
  url: https://localhost:8443/auth/login
  body:
    type: json
    data: |-
      {
      "username": "leo2",
      "password": "Test123!"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
