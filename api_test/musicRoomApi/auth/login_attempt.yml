info:
  name: login_attempt
  type: http
  seq: 5

http:
  method: POST
  url: https://localhost/auth/login
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
