info:
  name: new_account
  type: http
  seq: 5

http:
  method: POST
  url: https://localhost:8443/auth/new_account
  body:
    type: json
    data: |-
      {
        "username": "xekh",
        "password": "Test123!",
        "email": "@.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
