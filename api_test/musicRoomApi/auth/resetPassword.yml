info:
  name: resetPassword
  type: http
  seq: 12

http:
  method: POST
  url: https://localhost:8443/auth/reset-password
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
