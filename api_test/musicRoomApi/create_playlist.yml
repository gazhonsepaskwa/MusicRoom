info:
  name: create_playlist
  type: http
  seq: 15

http:
  method: POST
  url: http://localhost:80/playlists/create?title=tio&status=ekar&isPublic=True
  params:
    - name: title
      value: tio
      type: query
    - name: status
      value: ekar
      type: query
    - name: isPublic
      value: "True"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
