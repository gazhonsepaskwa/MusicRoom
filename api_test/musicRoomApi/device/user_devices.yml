info:
  name: user_devices
  type: http
  seq: 4

http:
  method: GET
  url: https://localhost/devices/user_devices
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
