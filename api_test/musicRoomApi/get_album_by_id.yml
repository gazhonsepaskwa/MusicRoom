info:
  name: get_album_by_id
  type: http
  seq: 6

http:
  method: GET
  url: http://localhost:80/album/8
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
