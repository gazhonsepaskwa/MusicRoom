info:
  name: get_playlist_by_id
  type: http
  seq: 14

http:
  method: GET
  url: http://localhost:80/playlists/1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
