info:
  name: get_profile
  type: http
  seq: 12

http:
  method: GET
  url: https://localhost/auth/profile
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
