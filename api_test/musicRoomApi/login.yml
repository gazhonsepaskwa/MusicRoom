info:
  name: login
  type: http
  seq: 11

http:
  method: POST
  url: http://localhost:80/auth/login
  body:
    type: json
    data: |-
      {
      "username": "a",
      "password": "dsfj"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
