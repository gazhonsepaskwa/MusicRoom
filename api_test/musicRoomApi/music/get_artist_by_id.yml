info:
  name: get_artist_by_id
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:80/artist/1
  body:
    type: json
    data: |-
      {
        "q" : "Kyougen - Ado"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
