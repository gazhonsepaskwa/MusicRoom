info:
  name: new_account
  type: http
  seq: 8

http:
  method: POST
  url: https://localhost/auth/new_account
  body:
    type: json
    data: |-
      {
      "username": "asg",
      "password": "dsfj",
      "email":"sjfdnf"
      }
  auth:
    type: bearer
    token: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
