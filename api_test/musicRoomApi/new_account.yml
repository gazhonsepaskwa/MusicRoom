info:
  name: new_account
  type: http
  seq: 5

http:
  method: POST
  url: https://localhost/auth/new_account
  body:
    type: json
    data: |-
      {
      "username": "asahazs",
      "password": "dsfj",
      "email":"sjddf"
      }
  auth:
    type: bearer
    token: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
