info:
  name: getFavorite
  type: http
  seq: 16

http:
  method: GET
  url: https://localhost:8443/playlists/favorite
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
