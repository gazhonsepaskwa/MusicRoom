info:
  name: profile
  type: http
  seq: 16

http:
  method: GET
  url: http://localhost:80/auth/profile
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
