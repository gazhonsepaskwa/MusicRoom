info:
  name: add_track
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:4242/add_track
  body:
    type: json
    data: |-
      {
        "q" : "je suis"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
