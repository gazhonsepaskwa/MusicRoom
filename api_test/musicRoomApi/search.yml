info:
  name: search
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:80/search?query=port&offset=1&limit=10
  params:
    - name: query
      value: port
      type: query
    - name: offset
      value: "1"
      type: query
    - name: limit
      value: "10"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
