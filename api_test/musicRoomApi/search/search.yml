info:
  name: search
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:80/search?query=hexa&offset=0&limit=10
  params:
    - name: query
      value: hexa
      type: query
    - name: offset
      value: "0"
      type: query
    - name: limit
      value: "10"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
