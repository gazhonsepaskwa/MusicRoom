info:
  name: Untitled
  type: http
  seq: 1

http:
  method: GET
  url: wss://localhost/socket.io/?EIO=4&transport=websocket
  params:
    - name: EIO
      value: "4"
      type: query
    - name: transport
      value: websocket
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
