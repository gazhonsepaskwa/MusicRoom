info:
  name: dfs
  type: websocket
  seq: 2

websocket:
  url: wss://localhost/socket.io/?EIO=4&transport=websocket
  message:
    type: text
    data: |
      {42["join_playlist","123"]}
  auth: inherit

settings:
  timeout: 0
  keepAliveInterval: 0
