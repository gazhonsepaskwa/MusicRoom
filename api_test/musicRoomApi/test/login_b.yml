info:
  name: login_b
  type: http
  seq: 4

http:
  method: POST
  url: https://localhost:8443/auth/login
  body:
    type: json
    data: |-
      {
      "username": "player_b",
      "password": "Password_b1@",
      "deviceID": "device_b",
      "deviceName": "device_b"
      }
  auth: inherit

runtime:
  variables:
    - name: deviceID
      value: suefhs
    - name: deviceName
      value: qsd

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
