info:
  name: new_account_a
  type: http
  seq: 3

http:
  method: POST
  url: https://localhost:8443/auth/new_account
  body:
    type: json
    data: |-
      {
      "username": "player_a",
      "password": "Password_a1@",
      "email": "mail_a@a.aa",
      "deviceID": "device_a",
      "deviceName": "device_a"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
