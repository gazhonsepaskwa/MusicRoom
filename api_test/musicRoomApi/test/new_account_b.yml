info:
  name: new_account_b
  type: http
  seq: 2

http:
  method: POST
  url: https://localhost:8443/auth/new_account
  body:
    type: json
    data: |-
      {
      "username": "player_b",
      "password": "Password_b1@",
      "email": "mail_b@b.bb",
      "deviceID": "device_b",
      "deviceName": "device_b"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
